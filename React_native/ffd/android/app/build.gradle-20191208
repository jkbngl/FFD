android {
  signingConfigs {
    release {
      storeFile file('ffd.keystore')
      storePassword 'ffdpassword'
      keyAlias 'ffd_alias'
      keyPassword 'ffdpassword'
    }
  }
  buildTypes {
    release {
      signingConfig signingConfigs.release
    }
  }
}